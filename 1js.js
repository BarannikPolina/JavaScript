const celsius = Number.parseInt(prompt ("Введите температуру в градусах Цельсия"));
alert(`Цельсий: ${celsius}, Фаренгейт: ${Math.round (fahrenheit = 1.8 * celsius + 32)}`);
function celsiusToFahrenheit(celsius) {
     return fahrenheit;
}